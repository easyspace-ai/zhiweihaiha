---
description: AIChat (/aichat) 开发规则与 OSINT 行为 parity 对照。修改 features/aichat 或 backend/application/aichat 时必须遵循。
globs:
  - frontend/src/features/aichat/**
  - backend/internal/application/aichat/**
  - backend/internal/interfaces/http/aichat_handler.go
  - e2e/aichat.spec.ts
alwaysApply: false
---

# AIChat 开发规则（摘要）

完整文档：`docs/aichat/PARITY_MATRIX.md`、`docs/aichat/DEVELOPMENT_RULES.md`

## 架构硬约束

- SSOT：`SessionEvent[]` / `conversation_events`；禁止 localStorage messages
- 单 SSE：`/api/aichat/sessions/:id/stream`
- Round 隔离；`follow_ups` 必须在 `round_sealed` 之后（INV-2）
- 复用 `osintdashboard` adapter，禁止复制 W6 runner

## 发送路由

- 推荐追问芯片 → `w6_manual`
- `@w6` → `w6_manual`
- `@edit-html` → `edit_html`（需选中 HTML 预览）
- 上下文条开 + 普通文本 → `discuss`（带 Markdown 报告）
- 上下文条关 → `deepseek`

## 改代码前

1. 查 `docs/aichat/PARITY_MATRIX.md` 对应条目
2. 共享 UI/工具在 `frontend/src/features/aichat/` 内维护

## PR 必过

- 更新 parity 表状态
- `go test ./internal/application/aichat/...` + `pnpm run test`（frontend aichat vitest）
- 手测：W6 完成→追问、报告预览、运行中停止、切会话
